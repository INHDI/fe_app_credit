import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Tín chấp - Conan",
  description: "Danh sách hợp đồng tín chấp",
};

export default function TinChapLayout({ children }: { children: React.ReactNode }) {
  return children;
}


